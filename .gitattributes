# Preserve imported atlas bytes so the asset manifest survives Git checkout.
wwwroot/archeon-atlas/** -text whitespace=trailing-space,space-before-tab,cr-at-eol
# The Chinese manuscript is a byte-for-byte snapshot of the SKBS source.
wwwroot/story.md -text whitespace=-blank-at-eol,cr-at-eol
# Keep upstream browser bundles and license text unchanged.
wwwroot/vendor/** -text whitespace=-blank-at-eol,cr-at-eol
