# Showheel

## Running the application
From the repository root, use the .NET CLI to start the application:

```bash
dotnet run
```

The default ASP.NET Core URLs are:
- `https://localhost:5001`
- `http://localhost:5000`

## Static files
Static assets are served from the `wwwroot` directory, with images under `wwwroot/images`.
